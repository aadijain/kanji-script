"""jp_tools: Japanese-English study utilities.

A shared core toolbox (tokenization, frequency, text, file parsing) plus
self-contained feature services (extraction today; furigana, sentence sorting,
... planned) exposed through thin adapters (cli / server / anki).
See memory/architecture.md for the map.
"""

__version__ = "0.1.0"
