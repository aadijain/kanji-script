"""Shared, feature-agnostic toolbox: text, tokenization, freq, file parsing.

Reused by every feature service; holds no feature-specific logic (e.g. vocab
extraction lives in jp_tools.services.extraction, not here).
"""
